---
description: 提示词自动注入、Skill 按需调用、choirboy 钩子三者不要混
alwaysApply: true
---

# 提示词 vs Skill vs choirboy

- **提示词**（`prompt-inject.md`）：每个新会话第一轮自动注入系统提示；之后只把注入折进 persona（已有则不再折一份）。不要把上一轮整段系统提示钉回去——梁神这类每轮追加 section 的预设会叠成线性膨胀（#29）。用户不必每场手动打「你是谁」来灌身份。
- **Skill**：环境装 `$DSH_HOME/skills`，工作区装 `<项目>/.dsh/skills`。同名时工作区盖住环境。调用走官方：聊天 `/名称`，或模型按 `description`/`whenToUse` 调 `skill` 工具。正文写「激活」无效。
- **choirboy-prompt** 在 `e:\渗透\choirboy-prompt`（仓库外，禁止提交）。它是 SessionStart 钩子自动灌记忆，不支持 DSH。
- Skill 功能只在测试版（≥1.1.12，且 channel≠stable）。正式版 1.1.11 无 Skill UI、无 skills.js。
- 设置侧栏：清洗/规则是「规则设定」，Skill 单独一栏且必须带本插件前缀（「规则 · Skill」）。不要写成光秃秃的 Skill，免得被当成宿主自带。不要把 Skill 折进系统提示。
- Skill 列表分「环境 / 工作区」。保存只改 SKILL.md，保留目录资源。官方字段：name、description、whenToUse、disable-model-invocation、user-invocable。
